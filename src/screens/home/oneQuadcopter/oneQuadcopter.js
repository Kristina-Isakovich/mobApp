import React from 'react';
import { Image, StyleSheet, View } from 'react-native';

export const oneQuadcopter = ({ item }) => {
  return (
    <View>
      <Image source={} />

    </View>
  );
}

const styles = StyleSheet.create({

});
